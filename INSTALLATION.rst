============
Installation
============

At the command line::

    $ pip install accuradiolib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv accuradiolib
    $ pip install accuradiolib

Or, if you are using pipenv::

    $ pipenv install accuradiolib

